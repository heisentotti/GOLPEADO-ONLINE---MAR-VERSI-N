services:
  - type: web
    name: golpeado-online
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn golpeado.server:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
